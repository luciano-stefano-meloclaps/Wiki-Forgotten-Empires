#!/usr/bin/env sh
. "$(dirname -- "$0")/_/husky.sh"

# 🔒 SEGURIDAD: Ejecutar detección de secrets antes del commit
npx lint-staged

# Ejecutar tests si existen
if [ -f "package.json" ]; then
  npm test 2>/dev/null || echo "⚠️  No tests configured or tests failed"
fi
